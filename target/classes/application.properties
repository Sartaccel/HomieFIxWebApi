# Application Name
spring.application.name=HomieFix

spring.datasource.url=jdbc:mysql://localhost:3306/db_homieFix?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=89036409

# Database Configuration (AWS RDS)
#spring.datasource.url=jdbc:mysql://talentturbo.cwcdihe45xpf.us-east-2.rds.amazonaws.com:3306/homiefix?useSSL=true&requireSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=homiefix_dev
#spring.datasource.password=DzvyMfqkOQRJ7RcB

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pooling (HikariCP - Optimized for AWS)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-timeout=30000

# Server Configuration
server.port=1111
server.error.include-message=always

# Spring Boot Settings
spring.main.web-application-type=servlet

# JPA Configuration (Optimized for Production)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Twilio Configuration (Consider using Environment Variables for security)
twilio.AccountSID=ACb582bd01b3b8e5455d76cf1bfae6ee6f
twilio.AuthToken=0de414a2f88a25a10d1bec253ce98d5a
twilio.PhoneNumber=+12563045312

# Email Configuration (Consider AWS SES instead of Gmail for better reliability)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sjshajai2002@gmail.com
spring.mail.password=sdcinlcaslueflnd
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# CORS Configuration (Allow Requests from Frontend in AWS)
spring.web.cors.allowed-origins=*

# Logging Configuration (Store logs in AWS-compatible location)
logging.level.root=INFO
logging.level.com.sart.HomieFix=DEBUG
logging.file.name=/var/log/homiefix/homiefix.log  # Standard location for AWS EC2
